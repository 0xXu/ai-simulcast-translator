{
  "name": "ai-simulcast-translator",
  "version": "0.1.0",
  "private": true,
  "description": "具备语义回溯能力的 AI 同声传译桌面应用",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": "^22.12.0 || >=24.0.0"
  },
  "scripts": {
    "dev": "corepack pnpm --filter @simulcast/desktop dev",
    "build": "corepack pnpm --filter @simulcast/desktop build",
    "ci": "node scripts/check-format.mjs && node scripts/lint-workspace.mjs && corepack pnpm -r typecheck && corepack pnpm -r test:run && corepack pnpm run build && corepack pnpm run test:e2e && node scripts/verify-demo.mjs",
    "format:check": "node scripts/check-format.mjs",
    "lint": "node scripts/lint-workspace.mjs",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "corepack pnpm -r test",
    "test:e2e": "corepack pnpm --filter @simulcast/desktop test:e2e",
    "test:run": "corepack pnpm -r test:run",
    "verify:demo": "node scripts/verify-demo.mjs"
  }
}
